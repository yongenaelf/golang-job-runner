# Golang Job Runner

A simple job runner written in Golang.

## How to run

```bash
go run main.go
```

## How to load test

```bash
cd load-test
python3 -m venv .venv
source .venv/bin/activate
pip3 install locust
```
